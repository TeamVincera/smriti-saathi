import type { Emit, Teardown } from './types.js';
interface InstallOptions {
    captureBodies: boolean;
}
export declare function installAllObservers(emit: Emit, options: InstallOptions): Teardown[];
export {};
