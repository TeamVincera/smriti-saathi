export type CommandHandler = (args: Record<string, unknown>) => unknown;
/** Query param appended on a hard reload to bypass the browser cache. */
export declare const RELOAD_CACHE_BUST_PARAM = "_reticle_reload";
export declare function resolveNavigationUrl(rawUrl: string, baseUrl: string): string | null;
/** Map browser command names to handlers. Used by the transport on each COMMAND. */
export declare function createCommandRegistry(): Map<string, CommandHandler>;
