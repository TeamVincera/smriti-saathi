/** Generated by scripts/embed-heroicons.mjs - do not edit by hand. */
/** Heroicons outline - MIT, https://heroicons.com */
declare const BODIES: {
    view: string;
    pointer: string;
    send: string;
    chart: string;
    'caret-down': string;
    check: string;
    remove: string;
    pause: string;
    play: string;
    stop: string;
    copy: string;
    download: string;
    message: string;
    gear: string;
    layout: string;
    trash: string;
    help: string;
    'caret-right': string;
    annotate: string;
};
/**
 * No `as const` on the values: it would inline every icon's path data into the emitted `.d.ts` as a
 * literal type, publishing the whole icon set twice for type information no caller can use. The KEYS
 * are still literal, which is the part `HeroIconBodyKey` needs.
 */
export declare const HERO_ICON_BODIES: Record<keyof typeof BODIES, string>;
export type HeroIconBodyKey = keyof typeof BODIES;
/** Solid variants for toolbar toggles - shown when data-active="1". */
declare const SOLID_BODIES: {
    chart: string;
    annotate: string;
    message: string;
    view: string;
    gear: string;
};
export declare const HERO_ICON_SOLID_BODIES: Record<keyof typeof SOLID_BODIES, string>;
export type HeroIconSolidBodyKey = keyof typeof SOLID_BODIES;
export {};
