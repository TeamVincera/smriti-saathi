/** Shared fixtures for the presenter test suites (presenter.test.ts + presenter-lifecycle.test.ts). */
export declare const FAST_IDLE_MS = 20;
export declare const FAST_FADE_MS = 5;
export declare const flush: () => Promise<void>;
export declare const wait: (ms: number) => Promise<void>;
/** Poll a predicate until true or timeout - robust to real-timer lateness under load (no flake). */
export declare const until: (pred: () => boolean, ms?: number) => Promise<boolean>;
export declare const dataOn: () => string | null;
export declare const dataBusy: () => string | null;
interface GlowFlips {
    enters: number;
    exits: number;
    stop: () => void;
}
export declare function trackGlowFlips(glow: HTMLElement): GlowFlips;
export {};
