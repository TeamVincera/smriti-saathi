/** Read the Vite-injected repo root, when present. */
export declare function readWorkspaceRoot(): string | undefined;
/** Last path segment for compact display (Codex/Cursor-style chip). */
export declare function workspaceFolderLabel(root: string): string;
/** Project id from the URL lease stamp, when present. */
export declare function readProjectIdFromUrl(): string | undefined;
export declare function workspaceSummary(): {
    folder: string;
    root?: string;
    projectId?: string;
};
/** Workspace chip + detail menu - sits above the composer row. */
export declare function workspaceRowHtml(): string;
export declare function paintWorkspace(root: HTMLElement): void;
export declare function mountWorkspaceSelector(root: HTMLElement): () => void;
