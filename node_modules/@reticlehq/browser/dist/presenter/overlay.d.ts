interface OverlayStats {
    connected: boolean;
    events: number;
}
export interface OverlayHandle {
    update: (stats: OverlayStats) => void;
    destroy: () => void;
}
/** A tiny in-page status chip: connection + event count. Off by default. */
export declare function installOverlay(): OverlayHandle;
export {};
