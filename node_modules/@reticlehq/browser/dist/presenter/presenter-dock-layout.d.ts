/** Flip floating panels and reclamp a dragged dock so everything stays on-screen. */
export declare function syncDockLayout(dock: HTMLElement, overlay: HTMLElement): void;
/** Batch dock layout work on the next frame (resize, panel open, drag end). */
export declare function scheduleSyncDockLayout(dock: HTMLElement, overlay: HTMLElement): void;
/** Resolve the dock node from an overlay root. */
export declare function findDock(overlay: HTMLElement): HTMLElement | undefined;
